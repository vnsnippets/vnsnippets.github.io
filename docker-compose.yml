services:
  hugo:
    image: ghcr.io/gohugoio/hugo:v0.160.1
    command: server --bind 0.0.0.0 --watch
    working_dir: /src
    ports:
      - "1313:1313"
    volumes:
      - .:/src
    environment:
      - HUGO_ENV=development